{
	"name": "api",
	"private": true,
	"scripts": {
		"dev": "./run-docker.sh",
		"test": "pytest",
		"lint": "flake8 src tests && mypy src tests",
		"format:write": "black src tests",
		"format:check": "black --check src tests"
	}
}
