{
	"buildCommand": "cd ../.. && turbo run build --filter={apps/site} --filter={apps/api} && cd apps/site && ./copy-api.sh && echo '--- ROOT ---' && pwd && ls -la && echo '--- API ---' && ls -la api || echo 'NO api dir'",
	"functions": {
		"api/index.py": {
			"memory": 512,
			"maxDuration": 10,
			"excludeFiles": "{*.{js,ts,json},@(.next|.turbo|node_modules|public)/**,src/{*,!(api)/**}}"
		}
	}
}
